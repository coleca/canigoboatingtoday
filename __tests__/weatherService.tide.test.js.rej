--- weatherService.tide.test.js
+++ weatherService.tide.test.js
@@ -162,9 +162,9 @@
     })
 
     test('throws an error if coordinates are invalid', async () => {
-      await expect(getTideData(91, -118.2437)).rejects.toThrow('Invalid coordinates provided.')
-      await expect(getTideData(34.0522, -181)).rejects.toThrow('Invalid coordinates provided.')
-      await expect(getTideData('34', -118.2437)).rejects.toThrow('Invalid coordinates provided.')
+      await expect(getTideData(91, -118.2437)).rejects.toThrow('Invalid latitude or longitude provided.')
+      await expect(getTideData(34.0522, -181)).rejects.toThrow('Invalid latitude or longitude provided.')
+      await expect(getTideData(null, -118.2437)).rejects.toThrow('Invalid latitude or longitude provided.')
     })
   })
 })
